import 'package:flutter/material.dart';

class Createpostscreen extends StatelessWidget {
  const Createpostscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('createpost');
  }
}
